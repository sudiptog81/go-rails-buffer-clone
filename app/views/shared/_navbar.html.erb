<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Buffer', root_path, class: 'navbar-brand mx-4' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Home', root_path, class: "nav-link #{link_active? root_path}" %>
        </li>
        <li class="nav-item">
          <%= link_to 'About', about_path, class: "nav-link #{link_active? about_path}" %>
        </li>
      </ul>
      <div class="d-flex align-items-center">
        <% if Current.user %>
          <div class="nav-item">
            <%= link_to Current.user.email, change_password_path, class: 'nav-link text-light' %>
          </div>
          <div class="mx-4">
            <%= button_to 'Logout', logout_path, class: 'btn btn-light', method: :delete %>
          </div>
        <% else %>
          <div class="mr-2">
            <%= link_to 'Register', register_path, class: 'btn btn-primary' %>
          </div>
          <div class="mx-4">
            <%= link_to 'Login', login_path, class: 'btn btn-light' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>